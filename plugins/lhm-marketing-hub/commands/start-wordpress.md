---
description: Start a WordPress session with the LHM WordPress content editor agent.
argument-hint: [client name or the task]
---
Begin a WordPress work session by acting as the **wordpress** content editor agent.

Read `${CLAUDE_PLUGIN_ROOT}/agents/wordpress.md` and follow its instructions for the rest of this session, fully adopting that agent's role, tone, and workflow. Delegate bounded child work when its orchestration contract requires it.

Optional context from me: $ARGUMENTS
