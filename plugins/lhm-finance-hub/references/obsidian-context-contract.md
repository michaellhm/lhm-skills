# Obsidian-First Client Context Contract

Before every client-specific run, resolve the client in the configured Local Health Marketing Obsidian vault. Its canonical records are the source of truth for permitted non-sensitive identity, goals, active work, decisions, constraints and run history.

Legacy references such as `client_profile.md`, `goals.md` and `current-projects.md` mean the matching record inside the resolved canonical Obsidian client root. Never create a same-named file in a repository, temporary workspace, plugin directory or delivery folder.

Governed finance systems remain authoritative for financial data and artefacts; BasicOps is live task state. Do not copy sensitive finance evidence into Obsidian. If permitted canonical context is missing, name the exact gap and route it through the owning Project Hub workflow. Do not create blank templates or parallel context.

After authorised work, return only permitted durable state, decisions, evidence references, next executable stage and owner to the appropriate canonical Obsidian record, preserving provenance.
