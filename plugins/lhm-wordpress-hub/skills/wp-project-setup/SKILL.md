---
name: wp-project-setup
description: "Initialize the canonical folder structure for a new WordPress website project. Use this when the user says 'new project', 'set up project folders', 'initialize website project', 'create project structure', or 'start fresh'. Detects whether shared client-root scaffolding exists; creates only the wordpress/ subtree if it does, otherwise creates both layers."
---

# Project Setup

Create the canonical folder structure for a new WordPress website project. The plugin convention separates shared client-level artefacts (cross-project reusable) from workflow-specific artefacts. This skill handles both layers.

## Step 1: Detect Project Location

Use the `AskUserQuestion` tool: "Where are we setting up this project?"

Options:
- **Inside an existing client folder** — current directory is already the client root (look for `client_profile.md`, `playbook.md`, or sibling `gmb/`, `landing-pages/` folders)
- **Create a new client folder** — ask for the client name, create `./[client-slug]/`
- **Project root is current directory but client root is one level up** — current dir is the wordpress folder

Detect intelligently:
- If CWD contains `client_profile.md` or `playbook.md` → current is client root, create `wordpress/` subfolder
- If CWD contains a `wordpress/` folder → already set up, ask if user wants to reset or work elsewhere
- If CWD has sibling `gmb/` or `landing-pages/` folders → current is client root
- Otherwise → ask the user explicitly via AskUserQuestion

## Step 1b: Record Platform Choice

If the platform choice was passed in from `wp-start`, use it directly. If not (e.g. this skill was invoked standalone), ask:

Use `AskUserQuestion`:
> "Which platform are we building on?"

Options:
- "WordPress (block theme — standard LHM build)"
- "Astro (static site framework — git-only workflow)"

Write `platform:` and the kickoff brief passed by `wp-start` into `client_profile.md` YAML frontmatter (create the frontmatter block if missing):

```yaml
---
business_name: ""
industry: ""
website: ""
primary_contact: ""
platform: wordpress   # or: astro
client_relationship: ""       # new | existing
strategy_call_required: ""    # yes | no
design_scope: ""              # redesign | improve | migrate
copy_scope: ""                # rewrite | selective | migrate
fixed_deadline: ""            # ISO date, constraint, or none
michael_approval_meetings: "" # case-by-case notes
---
```

> **Astro note:** Astro uses the seven-week delivery workflow in `wp-project-manager`. Do not route it through WordPress-only theme, Gutenberg, or SSH deployment steps.

## Step 2: Create Client-Root Scaffolding (if not present)

Check whether these files exist at the client root:

- `client_profile.md`
- `playbook.md`
- `website-brief.md`
- `clarifications.md`
- `design/` directory

If any are missing, create them with the templates in Step 4. Inform the user that Phase 1 (Steps 1.1–1.4) must populate these documents before Phase 2 can begin.

## Step 3: Create Build Subtree

> **Platform branch:** If `platform: astro` was recorded in Step 1b, skip the WordPress-specific structure below and create the Astro structure shown here. Then invoke `wp-project-manager` in Kickoff Create mode.

````
astro/
  seo/
    page_briefs/
  content/
    prototype/
    remaining/
  prototype/
  qa/
  docs/
    superpowers/
      specs/
      plans/
````

For **WordPress builds**, create `[client_root]/wordpress/` containing:

````
wordpress/
  seo/
    page_briefs/
  content/
    services/
    locations/
    supporting/
  prototype/
  wp/
    theme/
    blocks/
  qa/
    visual_qa_reports/
  docs/
    superpowers/
      specs/
      plans/
````

After scaffolding, invoke `wp-project-manager` in **Kickoff Create** mode to create `website-project-management.md` immediately. The PM doc must exist from the start so Krystalyn can schedule team work and client approval holds before strategy begins. Spec and plan references may remain `TBD` until they exist.

## Step 4: Create Starter Files at Client Root

### `client_profile.md`

````markdown
---
business_name: ""
industry: ""
website: ""
primary_contact: ""
platform: ""          # wordpress | astro
client_relationship: ""       # new | existing
strategy_call_required: ""    # yes | no
design_scope: ""              # redesign | improve | migrate
copy_scope: ""                # rewrite | selective | migrate
fixed_deadline: ""            # ISO date, constraint, or none
michael_approval_meetings: "" # case-by-case notes
---

# Client Profile

> Single source of truth for client context. Used by WordPress, landing pages, GMB, social, and any future workflow.

## Business Overview


## Target Audience


## Services / Offerings


## Locations / Service Areas


## Goals & Objectives


## Competitors
````

### `playbook.md`

````markdown
# Campaign Playbook — [Client Name]

> Generated by Phase 1 Step 1.3 (Campaign Playbook Skill). Approved version is the canonical reference for brand voice, services, USPs, and tone across every workflow for this client.

## Status

- **Approved by client:** No
- **Date approved:** —
- **Source transcripts:** strategy session ChatGPT transcript + Fathom recording

## Brand Voice


## Services


## USPs


## Tone of Voice


## Notes
````

### `website-brief.md`

````markdown
# Website Brief — [Client Name]

> Generated by Phase 1 Step 1.4 (Client Onboarding Skill). Defines scope, structure, and goals. Approved version locks scope.

## Status

- **Approved by client:** No
- **Date approved:** —

## Site Goals


## Target Audience


## Services and Pages Required


## Locations / Service Areas


## Calls to Action


## Design Preferences
````

### `clarifications.md`

````markdown
# Clarifications Log

Running log of questions asked and answers received during the project.

| Date | Question | Answer | Source |
|------|----------|--------|--------|
````

### `design/` directory (created empty)

`design/brand_guidelines.md`, `design/brand_style_guide.pdf`, `design/design_system.md` are produced by Phase 4. Do not pre-create these files — let the Phase 4 skills produce them.

## Step 5: Create `wp_state.md` Inside `wordpress/wp/`

````markdown
# WordPress Build State

Tracks the current state of the WordPress installation.

## Environment

- **URL**:
- **WP Version**:
- **PHP Version**:
- **Theme**:
- **Status**: Not started

## Pages Built

| Page | Content File | Status | WP Post ID |
|------|-------------|--------|------------|

## Plugins Installed

| Plugin | Purpose | Status |
|--------|---------|--------|

## Build Log

| Date | Action | Notes |
|------|--------|-------|
````

## Step 6: Confirm and Hand Off

Tell the user:

> Project structure created. Here's what's next:
>
> 1. **Project schedule created** — `website-project-management.md` now contains the standard seven-week active-delivery schedule, owners, dependencies, and client approval holds.
> 2. **Phase 1: Client Onboarding & Strategy** — Run the strategy session when required, generate the Campaign Playbook and Website Brief, and obtain client approval by email before SEO begins.
>
> Run `wp-start` when you're ready to begin.
