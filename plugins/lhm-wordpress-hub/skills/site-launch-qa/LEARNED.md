# Learned

<!-- Auto-maintained by Claude. Max 50 entries. Oldest/unused entries pruned after 3 months. -->

- (2026-07-29) Favicon QA must verify that a linked icon asset loads successfully and is client-branded; checking only for a `<link rel="icon">` tag can pass broken assets or framework defaults.
- (2026-07-29) On Cloudflare Pages, test redirects against production and inspect the full chain because a matching Pages Function can take precedence over rules in `public/_redirects`.
