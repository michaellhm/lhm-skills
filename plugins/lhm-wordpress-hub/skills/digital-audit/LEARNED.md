# Learned

<!-- Auto-maintained by Claude. Max 50 entries. Oldest/unused entries pruned after 3 months. -->
- (2026-07-31) `analytics-mcp`'s `run_report` rejects requests with no `dimensions` param, even for a pure-totals metrics query — always pass at least one dimension (e.g. `date`).
