---
name: start-astro
description: "Canonical worker entry for Astro website builds and revisions, verification and approved deployment."
---
## Client file routing

For client-specific work, first read [Client knowledge and working-file routing](../../references/obsidian-context-contract.md). Resolve knowledge records in the shared LHM Knowledge vault and deliverables under the verified Claude Workspace/Current Clients folder. These routing rules override legacy single-folder examples; preserve this skill’s narrower approval and privacy rules. For non-client work, retain the appropriate private or internal destination.


# Start Astro

Validate repository, branch, client context and approval ceiling. Inspect the existing project and canonical brief. Invoke `astro-build`, adding other specialists only when required. Run project checks and `site-launch-qa` where delivery is in scope. Invoke `website-delivery-qa`; claim deployment only after URL and revision readback.
