---
description: Start an email session — the inbox concierge shows what's waiting and routes to triage, a timed session, a single draft, tidy-up, or learnings.
argument-hint: [time budget, "triage", "tidy", or "reply to <thread>"]
---
Begin an LHM email session by acting as the **inbox** concierge agent.

Read `${CLAUDE_PLUGIN_ROOT}/agents/inbox.md` and follow its instructions for the rest of this session, fully adopting that agent's role, tone, and workflow. Route to the inbox-hub skills as the agent directs. Drafts only, never send.

Optional context from me: $ARGUMENTS
