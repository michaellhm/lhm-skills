# Hermes subscription Codex default — sealed change review

- Parent: `michael-codex-primary-routing-20260901`
- Incident: `hermes-openrouter-bypass-codex-subscription-20260901`
- Base: `f2d6e5584258fea75356b4726bb136ed395ffb89` (`origin/main`)
- Candidate branch: `cto/hermes-openrouter-bypass-codex-subscription-20260901`
- Return point: `desktop-control-review-sealed-codex-primary-routing-change-before-live-deployment`

## Recorded passes

1. **Capability Researcher — recommended.** Reused native LHM queue/watcher, structured-receipt, codexworker and Codex CLI patterns. No third-party dependency. The CTO lane and explicit Claude destination connectors remain separate.
2. **Platform Engineer — implemented.** Added an exact-schema container intake client, codexworker-only host worker, replacement path/service, validation, tests and release/rollback contract. Generic non-mutating, marketing and knowledge-work classes all select Codex. Only `default-review-only` is registered.
3. **QA Tester — pass with live limitations.** Targeted tests and repository validators pass. The full system-ops suite returned 188 passed and 34 subtests passed, plus three pre-existing CAP-015 assertions on unchanged files that expect release 0.9.6 while the canonical manifest is already 0.9.7.
4. **Security/Reliability Reviewer — approved for publication review.** The worker is the existing `codexworker` identity, receives only HOME/CODEX_HOME/PATH, checks ChatGPT login status, ignores user provider config, runs ephemeral/read-only, and cannot read the lhm_brain vault, root home or Docker sockets. API/OpenRouter/Anthropic variables are not inherited. Auth failure creates a durable incident and no alternate worker is called. Residual risk: prompt/tool-turn ceiling is reinforced by a 900-second hard process timeout and 16,000-character intake ceiling; live provider metering and subscription exhaustion cannot be proven in the source sandbox.
5. **Plugin Release Manager — sealed, not published or deployed.** Versions are Codex 0.9.51 and Claude/marketplace 0.9.72. The obsolete generic `lhm-codex-dispatch.path` is explicitly superseded by `lhm-codex-execution.path`; the CTO watcher is not replaced. Michael retains merge, release and deployment authority.

## Verification

- `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q plugins/lhm-system-ops/tests/test_codex_execution_bridge.py` — 8 passed.
- `PYTHONDONTWRITEBYTECODE=1 python3 plugins/lhm-system-ops/scripts/validate_system_ops.py` — passed, 12 skills.
- `python3 scripts/validate-plugin-versions.py` — passed.
- `python3 scripts/validate-script-parity.py` — passed.
- `PYTHONDONTWRITEBYTECODE=1 python3 -m pytest -q plugins/lhm-system-ops/tests` — 188 passed, 34 subtests passed, 3 pre-existing unchanged CAP-015 failures.
- `git diff --check` — passed.
- Sensitive-material scan — no credential material found; prohibited provider names occur only in negative tests/validation and the live regression instructions.

## Smallest consequential approval and rollout

One approval from Michael should name the immutable reviewed commit and authorise only: merge/release of lhm-system-ops; root-owned installation of the two executables and two units using the ownership/ACL contract; disabling an obsolete `lhm-codex-dispatch.path` only if it exists; enabling `lhm-codex-execution.path`; and running the redacted live smoke/regression suite. It does not authorise credential changes, Telegram downtime, main bypass, broader filesystem access, Claude-route changes or OpenRouter use.

Before installation record prior file/unit hashes, owners, modes, enablement, and the OpenRouter generation counter. Verify subscription status as codexworker without retaining output. Then test protected generic intake, marketing/knowledge default selection, auth failure, duplicate, invalid profile, timeout, existing website/repository gates, explicit Claude destination connectors and rollback. Acceptance requires zero new OpenRouter generation records and no hermes-2 worker inference during the controlled fail-closed regression.

Rollback is the exact procedure in `references/codex-execution-release.md`: disable only the replacement watcher, wait for its oneshot, restore the recorded inventory, daemon-reload, restore prior healthy watchers, and retain receipts/incidents.

## Live-regression repair — evidence-loop iteration 2

1. **Capability Researcher — recommended.** Native systemd executable-prefix privilege control and the existing LHM root-owned `setfacl` launch pattern are sufficient. No dependency, provider, credential, Hermes plugin, or third-party capability is added. The observed receipt `codex-smoke-generic-20260901-01` failed with EACCES, `parent_run_id: null`, and zero model inference because `/home/hermes/.hermes` returned to `mask::---` after queue submission.
2. **Platform Engineer — implemented.** The root-owned oneshot unit now runs three exact non-recursive `setfacl -n` preflights before its existing codexworker `ExecStart`. They restore only execute traversal for the named codexworker entry and ACL mask on `.hermes`, `lhm_brain`, and the Codex queue root. Queue ownership supplies the already-approved read/move/write access below that root; no vault, credential home, root home, shell, Docker, provider, Claude connector, website, or repository route changed.
3. **QA Tester — source pass; live replay pending deployment authority.** Targeted tests reproduce the launch ordering defect as a missing preflight and require the exact root-prefixed ACL commands before worker execution. Existing behavioural tests still cover the structured subscription-backed receipt, Codex selection for generic/marketing/knowledge work, invalid profile, duplicate, auth failure, provider-environment exclusion, read-only sandbox, and service isolation. Source QA cannot perform the live subscription smoke, timeout, route probes, or byte-for-byte OpenRouter counter comparison.
4. **Security/Reliability Reviewer — approved for publication review.** The permission delta is traversal-only on the same three paths already named by the release contract. `-n` prevents implicit mask recalculation; no recursive ACL or unrelated named ACL mutation occurs. The worker remains codexworker with `NoNewPrivileges`, strict filesystem protection, explicit vault/root/Docker denial, an allowlisted environment, ignored user provider configuration, read-only Codex sandbox, and no alternate-provider fallback. A privileged actor could reset the mask again after preflight; that fails the run closed and requires a new queue event or operator-controlled retry rather than provider fallback.
5. **Plugin Release Manager — sealed, not published or deployed.** Patch versions are Codex 0.9.52 and Claude/marketplace 0.9.73. The bounded publisher must create and report the exact candidate commit from these persisted files. Michael retains merge, release, root-owned installation, watcher activation, smoke, and rollback authority.

The smallest deployment approval remains one immutable-commit approval authorising only the lhm-system-ops merge/release, replacement of the four previously approved assets, root-owned creation/verification of the governed queue layout and three recorded ACLs, activation of `lhm-codex-execution.path`, and the redacted acceptance regression. Rollback must additionally restore the three pre-install ACLs byte-for-byte. It does not authorise live edits outside that inventory, credential changes, Telegram restart, OpenRouter use, Claude-route changes, broader access, or protected-main bypass.

## Incoming-file handoff repair — evidence-loop iteration 3

1. **Capability Researcher — recommended.** The native root-owned systemd preflight and POSIX default/file ACLs close the observed `codex-smoke-generic-20260901-02` boundary without a new dependency or identity. A default ACL alone cannot repair the already-created file that triggered the watcher, so the bounded handoff installs the default first and then repairs existing direct JSON entries.
2. **Platform Engineer — implemented.** Added `lhm-codex-queue-handoff`, invoked with systemd's root executable prefix after the three ancestor preflights and before the codexworker. It grants only named-user read plus an `r--` mask on `incoming` defaults and direct regular `*.json` request inodes. Descriptor-relative `O_NOFOLLOW` opens and `/proc/self/fd` ACL targets prevent symlink-swap escape. The helper does not recurse and does not alter groups, world access, schemas, providers, credentials, vault, Claude, repositories or worker authority.
3. **QA Tester — source pass; controlled live replay pending deployment authority.** The targeted suite reproduces the container helper's tempfile, chmod `0640`, and atomic rename and proves the inherited named-user read entry remains effective with `other::---`. It also requires exact root-preflight ordering and fail-closed controls. Repository validators and the full system-ops suite are recorded in the final handoff; source QA cannot perform subscription inference, route probes, timeout observation or OpenRouter counter comparison.
4. **Security/Reliability Reviewer — approved for publication review.** The permission delta is read-only for Codex request JSON in one already-governed incoming directory. The triggering-file/default-ACL ordering closes the watcher race; descriptor-relative no-follow handling closes path replacement. Rollback now records and restores the incoming directory's access and default ACLs in addition to the three ancestor states, byte-for-byte. Residual live risk is limited to operator ACL drift, which fails closed without provider fallback.
5. **Plugin Release Manager — sealed, not published or deployed.** Patch versions are Codex 0.9.53 and Claude/marketplace 0.9.74. The bounded publisher must create and report the exact candidate commit from the persisted files. Michael retains merge, release, installation, watcher activation and controlled regression authority.

The smallest consequential approval is one immutable-commit approval limited to the lhm-system-ops merge/release; root-owned installation of the existing client, worker and two units plus the new queue-local handoff helper; snapshot and verification of the three ancestor ACLs and incoming access/default ACL; activation of only `lhm-codex-execution.path`; and the redacted generic, marketing, knowledge, invalid-profile, duplicate, auth-failure and timeout regression with structured receipt checks and byte-identical OpenRouter counters. It excludes credential changes, Telegram restart, OpenRouter use, Claude/Hermes worker invocation, group membership changes, recursive ACLs, unrelated filesystem access and protected-main bypass.
