# SEO envelope executable integration — 2026-08-24

Parent: `lhm-seo-dept-pilot-2192596-20260824`; incident:
`CII-20260824-seo-envelope-executable-integration`; return point: Head of Production → SEO Lead →
first bounded specialist action.

## Recorded passes

1. Capability Researcher selected the native workflow controller, tracker connector, deployed
   `claude-dispatch` contract, and registered-adapter rehearsal boundary. No dependency was added.
2. Platform Engineer repaired root/container route parity and bound the keyword artefact bytes to
   one Drive create/readback, QA of that file ID/hash, Lead acceptance, canonical tracker CAS, and
   BasicOps readback. The packaged adapter invokes the controller's actual stdin CLI schemas.
3. QA Tester exercised the real entry with the deterministic adapter double, exact lineage,
   one-retry waiting receipts, route parity, negative bindings, controller security, and release tests.
4. Security/Reliability Reviewer verified fixed argv/executables, root-owned adapter packaging,
   disabled explicit service entry, immutable incident linkage, one Drive create, and rollback.
5. Plugin Release Manager refreshed versions and release hashes. Publication is reserved to the
   bounded root-owned publisher; merge, installation, enablement, and deployment remain unapproved.

No credentials or live connector systems were accessed during rehearsal.
