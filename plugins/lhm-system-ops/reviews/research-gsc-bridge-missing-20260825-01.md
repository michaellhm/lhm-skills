# Scheduled Research/GSC executor bridge — recorded role passes

Incident `research-gsc-bridge-missing-20260825-01`; parent `local-health-marketing-seo-20260824t2213z`; return point `scheduled_parent_ready_before_chief_intake`. Base is `origin/main` at `bd3f8ceb675ee1af4417424e8cc1d0d18b380b07`. This is source-only work on `cto/research-gsc-bridge-missing-20260825-01`; no commit, push, merge, deployment, unit enablement, cron change, live Hermes mutation, credential access, publication, indexing, GSC mutation, or contact occurred.

## 1. Capability Researcher — recommended

Inspected native Hermes/LHM capability before custom or third-party work. Selected the existing numeric Hermes container identity, `/opt/data` shared profile volume, registered `/usr/local/libexec/lhm-workflow-registered-adapter`, `seo_gsc_readonly`, organisational router/signers, tracker CAS, and work-control contracts. Rejected the prior branch history, direct bot credentials, bot tracker writes, Docker-group access, new plugins and any Search Console mutation route.

## 2. Platform Engineer — completed

Applied the reviewed executor scaffolding as a working-tree patch onto current main, then closed the missing boundaries. Every profile invocation is fixed to `/usr/bin/docker exec -i --user 10000:10000 hermes` and one exact `/opt/data/.local/bin/lhm_*` allowlist. Live execution first requires matching host and container metadata `10000:10000`, mode `0700`. Requests/results use a shared-volume per-child directory, delete any pre-existing result, and bind parent, child, role, canonical request SHA-256, artifact hashes and final result SHA-256.

Research runs the real Researcher for a bounded same-property absolute-URL plan, submits only that property and URLs through `submit-seo-gsc-readonly`, verifies the registered adapter binding and receipt/result hashes, writes immutable connector evidence, and invokes Researcher again for synthesis. Verifier stages invoke the real Verifier profile before an isolated per-role Ed25519 signer verifies exact artifact readback. Chief, Production, SEO, conditional Content/Website, Operations and Learning use their registered profiles. Operations proposes only the exact prior SHA-256 and replacement bytes; root performs atomic CAS and full byte readback. Work-control receives exact canonical stdin JSON only after the executable SHA-256 matches registration. One safe retry is persisted; the second identical child failure blocks the parent.

## 3. QA Tester — pass

- Focused controller: 35 passed.
- Applicable full controller suite: 168 passed, 1 skipped (environment-bound installed CLI and pre-staged candidate suites excluded and explicitly not claimed).
- System Ops: 158 passed plus 34 subtests. The excluded prototype release-parity file has an unrelated baseline fixture/readback mismatch.
- System Ops validator, plugin-version parity, script parity, Python compile, shell syntax and `git diff --check`: passed.
- Positive and negative fixtures cover numeric Docker identity, UID 1000 rejection, metadata mode, absolute same-property URLs, four-operation connector request/receipt hashes, malformed/binding/stale replay, tracker CAS/readback, exact work-control stdin/hash, duplicate intake/idempotency, interruption markers, one safe retry, signer isolation/failure, GSC failure, verifier role mismatch, and scheduler-accepted/business-result separation.

## 4. Security/Reliability Reviewer — approved

Permission ceiling remains read-only/non-production preview. Bots receive no credentials, signer keys, tracker write handle, deployment/publishing/indexing authority, GSC mutation tool, unrestricted shell or work-control argv. Root retains connector translation, evidence verification, CAS, exact readback and work-control. Result freshness is bound to the current canonical request and pre-existing bytes cannot satisfy a new invocation. Private signer keys are owner-isolated; Verifier must preserve exact registered evidence. Residual release risk is the authoritative live alias/UID/mode probe, adapter authorization and systemd sandbox verification, which activation deliberately performs separately.

## 5. Plugin Release Manager — ready for bounded publisher

System Ops plugin version is `0.9.18`; controller version is `0.1.11`; Claude/Codex manifests and marketplace are in parity. Release activation state is `source_only_disabled`; `LHM_WORKFLOW_ENABLE` defaults to `0`, signers and scheduled path units are explicitly disabled during provisioning, and no cron file is installed as active.

Activation after Michael separately approves merge/release: build the immutable reviewed commit, verify the release and critical-file SHA-256 manifest, run the root installer with the full release digest and default disabled state, retain installer output proving every scheduled/signer unit is disabled/inactive, run the live host/container metadata probes and registered GSC smoke, then separately authorize only the required path units. Rollback: disable/stop scheduled and signer path units, restore the retained `/opt/lhm-workflow/current` release and listed integration/unit backups with `packaging/rollback.sh`, verify cron remains unchanged and all source units inactive, then rerun focused regression and readback checks. Publication alone is not restoration; Michael retains merge, release and deployment authority.
