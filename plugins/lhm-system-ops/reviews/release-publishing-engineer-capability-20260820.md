# Release & Publishing Engineer — recorded role passes

Parent: `RELEASE-PUBLISHING-ENGINEER-20260820`  
Incident: `release-publishing-engineer-capability-20260820`  
Branch: `cto/release-publishing-engineer-capability-20260820`

## 1. Capability Researcher — native extension recommended

Native LHM already provides the bounded prototype publisher, exact workflow/public verification,
atomic receipts, independent QA route, canonical-state workflow and BasicOps mutation boundary. No
third-party plugin or universal GitHub credential is warranted. Extend the existing repository-
scoped route with governed destination profiles and preserve all other routes as disabled schemas.

## 2. Platform Engineer — implemented in the isolated workspace

Registered Release & Publishing Engineer beneath Head of Production with a concise skill contract
and UI metadata. Added destination profile schema version 1 and a registry in which only
`prototype-main-v1` is enabled under
`MICHAEL-PROTOTYPE-PUBLISH-STANDING-20260820`. Publication request/result version 3 binds the
profile, operation, fixed credential reference, authority, exact repository/branch and handoffs.

The publisher now preflights Git, SSH, GitHub CLI and durable receipt storage before checkout
mutation; discovers `gh` from the bounded service PATH; records the exact local release commit
before push; and resumes an interrupted post-push verification only when remote main equals that
same pending commit. Finalisation atomically stores one receipt with independent-QA, canonical
client-state and BasicOps Under Review contracts. Capability-restored event creation and durable
consumption share one validator and producer field set.

No commit, push, merge, installation, live publication, BasicOps/client-state mutation, credential
access, parent resume, capability-restored event or client contact occurred.

## 3. QA Tester — pass

`PYTHONDONTWRITEBYTECODE=1 pytest -q plugins/lhm-system-ops/tests` passed 83 tests and 19 subtests.
New tests cover unauthorized profiles, operations, credentials, authorities and production launch;
preflight failure before mutation; interrupted exact-push recovery without a second commit/push;
one durable receipt; open human review without client contact; and emitted capability-restored event
through validation, consumption and exact parent-role transition. Existing dispatcher, publisher,
release-parity, work-control, deployer, source-runtime and security regression coverage passed.

Skill quick validation, system-ops validation, plugin version parity, script parity, JSON/Python
syntax validation and `git diff --check` passed. Live host, GitHub, deployment, public HTTP,
BasicOps and Hermes checks remain installation/release-time tests and are not claimed.

## 4. Security/Reliability Reviewer — approved for feature-branch review

The request schema remains closed and has no command, arbitrary refspec, URL, workflow, DNS,
production-site or client-contact field. The enabled profile names one credential reference; it
does not contain credential material, and disabled profiles contain no credential reference or
authority. Repository identity, branch, operations, paths, hashes, size, symlink policy, sensitive
content, workflow identity, public bytes and authority fail closed. Pending and final records are
request-digest-bound, locked and atomically written. A moved or mismatched remote fails closed.

Residual risk: live executable/service PATH, credential file ownership, GitHub workflow identity,
branch protection and public endpoint behavior require the existing root installer/release
attestation. Rollback requires separate authority and reverts the exact release commit; it is not
granted to the employee.

## 5. Plugin Release Manager — ready for bounded branch publication only

Plugin version `0.7.0` is aligned across Codex, Claude and marketplace manifests. The workspace is
uncommitted on the required generated feature branch. The root-owned publisher may reconcile only
the reported changed files, commit and push only this `cto/*` branch, verify its remote SHA and
write Michael's review note. Michael's exact merge/install approval remains required afterward.
Publication of this review branch is not merge, installation, deployment, prototype publication or
capability restoration.
