# Hermes Monday SEO router

Use this as the native Hermes cron prompt. It performs routing only.

Read only the portfolio selection and runtime receipts. Never perform SEO analysis, call analytics/meeting/board MCPs, or write report content natively. All such work belongs to registered Claude CLI or Codex CLI workers.

Use Australia/Melbourne. Continue only Monday 08:00-08:09 local; otherwise exit silently. Read canonical `20 Clients/Client Flow.md`. Resolve the active cycle from the same verified anchor as Google Ads (Monday 3 August 2026 is Week 1, repeating every four weeks). Select the SEO-week column, excluding absent/unconfirmed scope and honouring recorded reschedules. Resolve client slugs from canonical registry, not generated names.

Read the approved SEO worker route configuration and prior run ledger. If research or delivery is not registered and verified for SEO, return ROUTE_UNAVAILABLE with stage and missing capability. Never substitute an Ads-only route or no-MCP route, never fall back to native Hermes, and never create a success digest without reports.

Process each selected client sequentially. Dispatch the registered research worker with `lhm-gmb-hub:monthly-cycle-report`, last complete calendar-month comparison, fresh meeting and personal-board reconciliation, per-location stage, goals, and the three-file report contract. Preserve run ID before polling; a queued receipt is not completion. Resume existing work rather than duplicate it.

Pass verified output to the registered CLI delivery worker with the canonical Drive destination, verified SEO-owner Inbox, and client-period dedupe key. Read back all files and the task discussion/owner/section. Link existing tasks rather than creating duplicate execution subtasks. Never implement proposed live SEO changes.

After all children finish or report an explicit failure, dispatch the digest worker once with verified highlights and links. Production is To the configured Jaimee address, CC the configured support address. Include failed clients and their exact gaps. Require a stored send intent and message receipt; reconcile uncertain outcomes rather than resend. Return only client/run statuses, Drive/task URLs and email delivery state.

A manual one-client test may waive the time/selection guard only when explicitly requested. Its mode must be test, named client only, Michael Inbox and Michael-only email. Do not inherit production recipients. Keep the production schedule paused until the full worker route and requested pilot acceptance have passed.
