# One feedback conversation, existing project records

The email footer invites the reader to open their existing BasicOps chat with Lily and paste:

“Update the weekly web brief for [week/date]. Here are my project corrections: [notes]. Update the existing BasicOps discussions and Obsidian project records, then tell me what changed and what still needs my decision.”

A voice note/transcript or plain message listing several clients is fine. Process it project by project without making the user visit every card. This skill also works in another authenticated LLM with the same connected records. Do not advertise automatic email-reply processing: the current Reply-To is Michael, not a monitored Lily ingestion route.

1. Authenticate sender and resolve each named client/project. Read existing task discussions, relevant email and canonical notes first; preserve work completed since the report cutoff.
2. Separate factual updates, actions explicitly requested, proposed new work, and style feedback. A comment such as “old cards need cleanup” is not authority to close every old card. Resolve precise candidates before consequential changes.
3. Apply clear authorised corrections through basicops-task-manager and wp-project-manager (or lp-project-manager). Use existing cards; avoid duplicates. Preserve original dates, approval provenance and history. Do not convert estimates into promised deadlines. Human completion/assignment decisions must follow the owning skill and the authenticated user's authority.
4. Before ANY BasicOps message in Lily's name, verify authenticated user_id=82484. Stop that send if authenticated as Michael or another human; never impersonate Lily through text. Other authenticated assistants identify themselves accurately. Route actionable context to Discussion, never prose in Description. Follow personal Inbox routing and readback rules in basicops-task-manager.
5. Record the user's actual correction, source/date, current state, owner, next action and date basis in the canonical project note; keep Current Projects aligned. Never overwrite intervening changes blindly.
6. Verify records after writes. Return a short “Updated / Still need your decision” receipt with native task links and note references. If one update is ambiguous, finish unrelated clear ones and ask one concrete question about the unresolved item.
7. Changes to layout/style are skill-maintenance requests through the canonical repository. Keep temporary client facts out of the reusable skill. Do not silently edit installed caches.

Report generation is read-only. The feedback instruction above explicitly requests record updates when the user invokes it; mere receipt of the weekly email does not authorise mutations, dispatch, launches or client contact.
