Run weekly-web-project-brief in scheduled mode using the attached gate result. If wakeAgent is false, stop silently. Otherwise use its week and output_directory; read the skill and all three references, research the current full website portfolio, save the research receipt and brief.json, render the HTML/text email, run the pre-send checks, send through the fixed receipt-backed Mailgun helper, and verify recipient delivery. Do not create or change project tasks, contact clients, or dispatch production. Do not use the previous email as current evidence. Do not change this job, recipients or skill. If core sources fail, save and report the failure instead of sending a guessed portfolio. Return a concise local receipt with week, coverage, message ID and actual delivery state.

Mandatory: Codex CLI must load this exact skill, prove live Gmail/Obsidian/BasicOps read access, obtain current primary evidence and compare against the last approved brief/corrections. Stop on material source gaps or an unavailable registered worker lane. Never substitute an offline partial reconstruction or default Hermes model. Retain all freshness/review files and pass quality.py before sending.
