Run weekly-web-project-brief in scheduled mode using the attached gate result. If wakeAgent is false, stop silently. Otherwise use its week and output_directory; read the skill and all three references. Hermes must acquire the current read-only BasicOps boards 68635 and 68921, Obsidian and Gmail evidence and save research-receipt.json. If a core source fails, save and report the failure and do not submit or send. Submit only that scoped evidence through the registered weekly_web_project_brief / weekly-web-brief-json-v1 Codex dispatch contract, poll its receipt, require selected_worker=codex, workflow=weekly-web-project-brief, final_status=completed and the exact gate week, then save its brief object as brief.json. Render the HTML/text email deterministically, run the pre-send checks, send through the fixed receipt-backed Mailgun helper, and verify every recipient delivery. Do not draft with Hermes/default DeepSeek, create or change project tasks, contact clients, dispatch production, use the previous email as current evidence, or change this job, recipients or skill. A missing, failed or mismatched Codex receipt is a hard no-send condition. Return a concise local receipt with week, coverage, Codex child receipt, message ID and actual delivery state.
